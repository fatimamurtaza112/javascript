import { add } from './maths.js';
import addition from './maths.js';

let sum = add(3, 4)
console.log(sum)
sum = addition(4, 5)
console.log(sum)

//import export == named and default




// linking file
// same file
// one level up
// one level deep
